server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://mysql:3306/springbankmanagementsystem
    username: root
    password: 532001
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect